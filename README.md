# Pokemon Stock Finder — Full Backend with Admin & FCM

This package includes:
- Async scrapers for major AU stores (placeholders)
- Database caching (MongoDB or file-based)
- Admin dashboard (basic)
- Firebase Cloud Messaging (FCM) integration (server-side)
- Dockerfile & GH Actions template

## Env vars
- MONGODB_URI (optional)
- CACHE_TTL (seconds)
- PROXIES_JSON (optional)
- FIREBASE_SERVICE_ACCOUNT_JSON (JSON string of service account)
- FCM_TOKENS_JSON (JSON array of device tokens for testing)
